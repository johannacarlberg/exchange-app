import React from 'react';
import { shallow } from 'enzyme';
import { ConnectedExchangeButton } from './ExchangeButton';

describe('ConnectedExchangeButton', () => {
  it('disabled', () => {
    // const updateToBalance = jest.fn();
    // const updateFromBalance = jest.fn();
    // const updateStatement = jest.fn();
    // const fromValue = 10;
    // const button = shallow(<ConnectedExchangeButton from="GBP" to="EUR" updateToBalance={updateToBalance} updateFromBalance={updateFromBalance} updateStatement={updateStatement} />);
    // console.log(button.instance())
    // console.log(button.debug())
    //
    // expect(button.prop('disabled')).toBe(true);

  });
});
